spring.application.name=event-registration

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/event_registration_portal
spring.datasource.username=root
spring.datasource.password=Maesecret@99sql

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.application.name=event-registration

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/event_registration_portal
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8084
eureka.client.service-url.defaultZone=https://eureka-service-s7wp.onrender.com/eureka/

# JWT Configuration (Must match Auth Service exactly)
college.app.jwtSecret=${JWT_SECRET}
college.app.jwtExpirationMs=86400000
